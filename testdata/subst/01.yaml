---
apiVersion: v1
kind: Pod
metadata:
  name: $APP_NAME
  labels:
    app: nginx
spec:
  containers:
    - name: $APP_NAME
      image: ${APP_IMAGE_NAME}:${APP_IMAGE_TAG}
      ports:
        - containerPort: 80
